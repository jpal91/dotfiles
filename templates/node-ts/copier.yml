project_name:
  type: str
  question: What is the project name?

_tasks:
  - command: "pnpm add -D typescript tsx @tsconfig/node-lts @types/node"
    when: "{{ _copier_operation == 'copy' }}"
    working_directory: "{{ project_name }}"

_exclude:
  - copier.yml
